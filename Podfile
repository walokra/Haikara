source 'https://github.com/CocoaPods/Specs.git'
platform :ios, '8.0'
use_frameworks!

def shared_pods
	pod 'Alamofire', '~> 2.0'
end

target 'highkara' do
	shared_pods
	#pod 'FLEX', '~> 2.0', :configurations => ['Debug']
end

target 'NewsWidget' do
	shared_pods
end
